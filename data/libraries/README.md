# Place compound libraries here for virtual screening (Step 06).
#
# Supported formats: .smi, .csv, .smi.gz
# Example: Enamine REAL subset, ZINC download
#
# Usage:
#   python scripts/06_virtual_screening.py \
#       --library data/libraries/compounds.smi \
#       --model models/best_model.joblib
