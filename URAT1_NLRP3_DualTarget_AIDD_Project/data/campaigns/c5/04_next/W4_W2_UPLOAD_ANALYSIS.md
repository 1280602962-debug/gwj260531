# 上传分析：W4 146 对接 + W2 IFP（commit `c37f99ad`）

分析日期：2026-09-05。上传本身没有结构门打分；本目录的 W4 数字来自随后的零对接复算（`scripts/score_c5_w4_nlrp3_panel.py`）。

---

## 上传了什么

| 项 | 状态 |
|---|---|
| W4 配体准备（Dimorphite→Meeko） | 8 阳性 + 40 诱饵，协议正确 |
| W4 gnina 设置 | 7ALV，盒锁定，exh=32，9 modes，rescore，seeds 42/43/44，`--no_gpu`，二进制 1.3.2 |
| W4 job 数 | 146 已排队；**131 ok / 15 fail** |
| W2 IFP | 0 新对接；228 vs 64 已评一次 |
| W4 结构门打分 | 上传时**没有**；现已补 |

---

## W4：15 个失败格

5 个诱饵 × 3 种子，SDF 为空。日志只有 gnina 启动横幅，没有姿态表，像是超时/中途退出（runner 超时 7200 s）。

| decoy | 备注 |
|---|---|
| C5W4D_002 | 金刚烷酯 |
| C5W4D_022 | 螺环乙内酰脲 |
| C5W4D_026 | 大环内酰胺（难采样） |
| C5W4D_027 | 磺酰负离子 |
| C5W4D_029 | 稠杂环酰胺 |

阳性 9×3 全有姿（含复用的 NP3-146）。`REP_07837` seeds 43/44 已补上。

空 SDF 记为宽松门和结构门都不通过（没有姿态 = 没进口袋）。完整案例（丢掉这 5 个）结论不变。

**不必为了过门再重跑这 15 格。** 若只为 SI 完整矩阵，可原设置重试一次；再失败就保持“无姿态”。

---

## W4 结构门（已复算，阈值未改）

冻结门槛与 C1 相同：overlap≥0.50，IFP≥0.50，关键接触≥5/7，无冲突。没看名单后调参。

**主对照 = 9 阳性 vs 40 诱饵（seed 42，失败诱饵记不通过）：**

| 门 | 阳过 | 诱饵过 | Fisher p |
|---|---:|---:|---:|
| 宽松（COM≤6 Å + CNNscore≥0.5） | 9/9 | 15/40（37.5%） | 6.4×10⁻⁴ |
| **结构** | 9/9 | **11/40（27.5%）** | **8.2×10⁻⁵** |

结构门比宽松门少放进 4 个诱饵（C5W4D_028/036/037/039），阳性一个没丢。三种子同一方向。完整案例 n=35 仍是结构门更特、p<0.05。

预登记通过规则（结构特异性 > 宽松，且 p<0.05）：**通过。**  
OR 因 fn=0 为无穷，正文写“阳性全回收 + Fisher p”，不要写无限比值当卖点。

**对 20 个临床酸背景：结构门和宽松门仍然逐行相同**（seed42 都是 12/20）。M2 在“随机临床酸”上没修好；W4 要修的是性质匹配诱饵，这一块修好了。

产物：`w4_panel_metrics_all_seeds.csv`、`w4_structural_gate_summary.json`。

---

## W2：数字过门，增量很小

锚在沉积 R75/A1AIL/A1A45（Kabsch 到 9DKB），Arg 上限仍是 A1AIL 的 7.7027 Å，没有用苯溴马隆/TD-3 的远距离放宽。Q437 在 9DKB 是 LEU，11/12 残基。A1A45 变换后 IFP 带 `CLASH:ANY`，已并进 union，要在正文当局限写。

主读出（CNNscore Top-1 + 结构）：OR=3.15，CI95=[1.72, 7.08]，p=6.7×10⁻⁴，**gate_pass=true**。

和已有 A1（OR=3.18）比：IFP 是 A1 的严格子集。292 个分子里 **0 个**“IFP 过、A1 不过”；只有 7 个“A1 过、IFP 不过”（6 活性 + 1 诱饵），全死在 IFP Jaccard + 关键接触。诱饵假阳性 13→12。best-of-9 IFP 的 CI 下界 0.94，**过不了**预登记，只作诊断。

HANDOFF 里先写 `gate_pass=false` 再写 `true`，以 `w2_ifp_gate_summary.json` 为准。

**不要再调 W2 阈值。** 正文：URAT1 判别仍以 A1 为主；IFP 是更严的确认子集，不是新的排序器。

---

## 接下来干什么

| 优先级 | 做不做 | 新对接 |
|---|---|---|
| W4 结构门打分 | **已在本分析做完** | 0 |
| 重试 15 个失败诱饵 | 可选 SI | 最多 15 |
| W1 其余 29 格 | 仍可选，不当推进 | 29 |
| 临床 156 / Rank 轨重对接 | **不做** | 0 |
| 给 URAT1 另开排序分 | **不做** | — |
| **短名单冻结** | **下一步** | 0：在已有 clinical 9DKB/7ALV SDF 上标 W2 IFP；tier-2 去掉 β-内酰胺 |
| 重写投稿正文 | 短名单冻结后 | 0 |
| W5 MD | 冻结后才授权 | 0（MD 不是对接） |

**本机不必再为过门开对接。** 计算主线从“补对接”转到“冻结短名单 + 写正文”。
