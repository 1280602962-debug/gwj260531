# URAT1 capability docking products

Local D1/D2 outputs. How to run: `docs/URAT1_CAPABILITY_DOCK.md`.

`d1_job_inventory.csv` is the 27-cell matrix. `d1_capability_summary.csv` is updated after `--score-only` or a GNINA run. Seed directories and SDF/log files stay untracked.
