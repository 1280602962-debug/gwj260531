# Docking score tables

Production engine is **gnina P2** (`config/docking_production_p2.yaml`).

Historical Glide XP exports (8973 distill retrospective and dual-target merges) have been **removed**. Do not restore them into ranking tables or figures.

After `bash scripts/run_funnel_p2.sh`, P2 dual-dock scores live in `results/repurposing/` (gitignored by default).
