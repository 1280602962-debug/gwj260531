# Candidate nomination outputs

Write here after P2 dual-dock scores exist:

```bash
python3 scripts/14_candidate_nomination.py --tau 90 --mw-min 200 --mw-max 550
```

Do not commit Glide-era shortlists. Current follow-up molecules are listed in `docs/MANUSCRIPT.md`.
