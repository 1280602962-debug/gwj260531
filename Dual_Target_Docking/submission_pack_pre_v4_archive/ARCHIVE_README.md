# Pre-V4 submission pack archive

This directory is the pre-consolidation `submission_pack/` as it existed at
`34a511a537e8af3be98843b2276455435f27e4f1`, before the V4 postfix figure/SI
renumbering. It is retained for provenance. The active pack is `submission_pack/`.
