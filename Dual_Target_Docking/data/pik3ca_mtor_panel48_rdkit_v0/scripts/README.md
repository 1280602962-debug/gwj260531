# Recovered PIK3CA/mTOR RDKit prep script

`pm48_rdkit_prep_dock.py` was restored from git commit `c213c485`. It is a method snapshot (ETKDGv3 seed 20260727, MMFF 200, Meeko, Vina E=16).

The script hard-codes a historical ROOT:

`/mnt/d/CADD paper exercise/dual target docking/results/pik3ca_mtor_panel48_rdkit_v0`

That path is **historical / unavailable** as a current instruction. Exact production ligand PDBQT are not in git. Current scores: `tables/ablation_ligand_scores.csv`.
