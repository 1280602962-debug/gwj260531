# Archived v3 builders

`legacy_v3_builders.py` holds unused fig4/fig5/fig6/figS7/figS8 builders and the old in-module verify(). They must not write official artwork.

Use `update_figures_pr32.py` and `plotted_values_postfix.json`.
